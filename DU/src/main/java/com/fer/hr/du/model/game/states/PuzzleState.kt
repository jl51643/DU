package com.fer.hr.du.model.game.states

/*import kotlinx.coroutines.flow.MutableStateFlow


data class PuzzleState constructor(
        val puzzles: List<MutableStateFlow<Puzzle>> = listOf(
                MutableStateFlow(Puzzle(color = Color.Cyan, isFocused = false)),
                MutableStateFlow(Puzzle(color = Color.Green, isFocused = false)),
                MutableStateFlow(Puzzle(color = Color.Red, isFocused = false))
        ),
)

data class Puzzle(
        val color: Color,
        val isFocused: Boolean
)*/
